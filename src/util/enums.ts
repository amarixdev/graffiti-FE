export enum Button {
  enabled,
  disabled,
}

export enum SocketType {
  user,
  remote,
}

export enum Page {
  canvas,
  community,
}

export enum RequestMethod {
  post,
  update,
}

export enum CanvasState {
  new,
  edit,
}

export enum Previews {
  collection,
  single,
}
